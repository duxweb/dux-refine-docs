import { IslandPlugin } from 'islandjs/plugin';

interface SearchOptions {
    maxSuggestions?: number;
}
declare function pluginSearch(options?: SearchOptions): IslandPlugin;

export { SearchOptions, pluginSearch };
