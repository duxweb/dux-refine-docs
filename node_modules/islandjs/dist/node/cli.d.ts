import { UserConfig } from 'vite';

interface CLIDevOption extends UserConfig {
    force?: boolean;
    config?: string;
    '--'?: string[];
}
interface CLIBuildOption extends UserConfig {
    config?: string;
    force?: boolean;
    sourcemap?: boolean | 'inline' | 'hidden';
}

export { CLIBuildOption, CLIDevOption };
